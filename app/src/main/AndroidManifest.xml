<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asus.eduaction">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="高考推荐系统"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".test_sub"
            android:label="专业测试" />
        <activity
            android:name=".LoginActivity"
            android:label="登录">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".resultActivity"
            android:label="结果" />
        <activity
            android:name=".registerActivity"
            android:label="注册" />
        <activity
            android:name=".first_tuActivity"
            android:label="查询系统"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".first_coActivity"
            android:label="推荐系统" />
        <activity
            android:name=".lineActivity"
            android:label="推荐系统" />
        <activity android:name=".s_resultActivity" />
        <activity android:name=".emptyActivity" />
        <activity android:name=".l_resultActivity" />
        <activity android:name=".hotActivity"></activity>
    </application>

</manifest>